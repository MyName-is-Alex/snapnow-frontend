import { View, Text } from "native-base";
import { ScreenProps } from "../../screen/appScreens";

const ChatList = ({ navigation }: ScreenProps) => {
    return (
        <View>
            <Text>Chat is working</Text>
        </View>
    );
};

export default ChatList;
