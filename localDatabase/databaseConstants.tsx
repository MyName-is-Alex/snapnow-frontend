enum dbNames {
    mainDb = "main",
}

enum dbTables {
    authDb = "authentication",
    messagesDb = "messages",
}

const databaseConstants = { dbNames, dbTables };

export default databaseConstants;
